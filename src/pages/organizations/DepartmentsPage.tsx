import { ArrowLeft, Plus, Search } from "lucide-react";
import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { Drawer, PageHead, Panel, Status } from "@/components/ui/ToolKit";
import { departments } from "@/lib/staticData";
export function DepartmentsPage(){const nav=useNavigate();const [selected,setSelected]=useState(0);const [q,setQ]=useState("");const [drawer,setDrawer]=useState(false);const rows=departments.filter(x=>(x.name+x.code).toLowerCase().includes(q.toLowerCase()));const d=departments[selected]||departments[0];return <div className="page"><PageHead eyebrow="Organization / Functional structure" title="Departments" description="Functional teams stay separate from the organization-unit hierarchy and can be assigned across relevant units." actions={<><button className="btn" onClick={()=>nav("/organizations/1")}><ArrowLeft size={13}/> Command center</button><button className="btn primary" onClick={()=>setDrawer(true)}><Plus size={13}/> New department</button></>}/><div className="toolbar"><div className="searchbox"><Search/><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search departments…"/></div><div className="grow"/><span className="ref">{rows.length} DEPARTMENTS</span></div><div className="split wide-inspector"><Panel title="Department register" kicker="Kumar Constructions"><table className="data-table"><thead><tr><th>Department</th><th>Lead</th><th>Units</th><th>Members</th><th>Status</th></tr></thead><tbody>{rows.map((x)=><tr key={x.code} className={d.code===x.code?"selected":""} onClick={()=>setSelected(departments.findIndex(y=>y.code===x.code))}><td><div className="ref">{x.code}</div><strong>{x.name}</strong></td><td>{x.lead}</td><td>{x.units}</td><td>{x.members}</td><td><Status tone="green">{x.status}</Status></td></tr>)}</tbody></table></Panel><Panel title={d.name} kicker="Department inspector" className="inspector"><div className="inspector-section"><dl className="keyvals"><dt>Reference</dt><dd className="ref">{d.code}</dd><dt>Lead</dt><dd>{d.lead}</dd><dt>Assigned units</dt><dd>{d.units}</dd><dt>Members</dt><dd>{d.members}</dd></dl></div><div className="inspector-section"><div className="inspector-label">Unit assignments</div>{["Mumbai Region","Western Projects","Skyline Site Office"].slice(0,Math.min(3,d.units)).map(x=><div className="tree-node" key={x}><span className="tree-dot"/><span>{x}</span></div>)}</div><div className="inspector-section"><button className="btn" style={{width:"100%"}}>Manage unit assignments</button></div></Panel></div>{drawer?<Drawer title="Create department" onClose={()=>setDrawer(false)} footer={<><button className="btn" onClick={()=>setDrawer(false)}>Cancel</button><button className="btn primary" onClick={()=>setDrawer(false)}>Create department</button></>}><div className="field"><label>Department name</label><input className="input" placeholder="e.g. Planning"/></div><div className="field" style={{marginTop:12}}><label>Department lead</label><select className="select"><option>Select organization user</option><option>Rohan Mehta</option></select></div></Drawer>:null}</div>}
